# Example configuration for PccSecurityCheckLovableStack.
#
# Setup:
#   cp config/supabase.example.properties config/supabase.properties
#   # then fill in real values below
#
# The real `supabase.properties` is gitignored — never commit credentials.
# Override the config path at runtime via:
#   -Dsupabase.config.file=/abs/path/to.properties
#   or env SUPABASE_CONFIG_FILE=/abs/path/to.properties

# Project URL — find under Supabase dashboard > Project Settings > API.
# Format: https://<project-ref>.supabase.co
supabase.url=https://YOUR-PROJECT-REF.supabase.co

# Anonymous (anon) public API key. Safe to ship in frontends.
# Used by the security checks to simulate an unauthenticated client.
supabase.anon.key=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.REPLACE_ME.REPLACE_ME

# ---------------------------------------------------------------------------
# IMPORTANT — Service role key is NOT read from this file.
#
# The service role key bypasses RLS and grants full DB access. It must never
# live on disk in the working directory. Provide it at runtime instead:
#
#   env:  SUPABASE_SERVICE_ROLE_KEY=eyJh...
#   jvm:  -Dsupabase.service.role.key=eyJh...
#
# In IntelliJ: run configuration -> "VM options" or "Environment variables".
# On the command line:
#   SUPABASE_SERVICE_ROLE_KEY=eyJh... mvn -q compile exec:java@all
# ---------------------------------------------------------------------------

# Optional: short project ref (e.g. xyzabc). Inferred from URL if absent.
# supabase.project.ref=

# Optional HTTP timeouts (seconds).
# http.connect.timeout.seconds=10
# http.request.timeout.seconds=20
